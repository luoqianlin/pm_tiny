#include <cstdlib>
#include <iostream>

int main() {
    std::cerr << "pm_tiny Windows stub build: functionality not yet implemented." << std::endl;
    std::cerr << "The executable only validates compilation on Windows." << std::endl;
    return EXIT_FAILURE;
}
